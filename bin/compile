#!/bin/bash

BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3

indent() {
  sed -u 's/^/       /'
}

bin/clean $BUILD_DIR | indent

bin/rdfind -makehardlinks true -outputname $BUILD_DIR/.rdfind.txt $BUILD_DIR | indent